/**
 * File upload middleware — STUB
 * Full implementation: Stage 11 (Health Reports)
 *
 * Multer configuration will be wired here.
 * Reads UPLOAD_DIR and MAX_FILE_SIZE_MB from environment variables.
 */

// Placeholder — multer config added in Stage 11
module.exports = {};
